import { FunctionComponent } from 'react'
import { Grid } from '@mui/material'

const NewFeed: FunctionComponent = () => {
  // useEffect(() => {
  //   dispatch(newfeedActions.getNewfeed())
  // }, [])
  return <Grid container></Grid>
}

export default NewFeed
